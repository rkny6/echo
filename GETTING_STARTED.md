# 新手安装教程（完全不会写代码也能看懂）

这份教程假设你**从来没写过代码**，只是想把这个项目跑起来看看效果。所有专业名词第一次出现时都会解释一下是什么意思。

## 开始之前，你需要确认一件事

**你必须有一台 Mac 电脑**（MacBook / iMac / Mac mini 都可以）。

这不是这个项目的限制，而是苹果的规定：开发 iPhone App 必须用苹果自己的工具「Xcode」，而 Xcode 只能装在 Mac 上，Windows 电脑装不了。如果你手头只有 Windows 电脑，很遗憾这份教程没法继续往下走。

---

## 第一步：安装 Xcode（苹果的开发工具）

1. 在 Mac 上打开「App Store」（就是那个蓝白色的 App 图标）
2. 搜索 **Xcode**
3. 点击「获取」/「安装」，它是免费的，但体积很大（几十 GB），需要耐心等它下载完
4. 安装完成后，打开一次 Xcode，它可能还会再弹窗要求安装一些「附加组件」，一路点同意/安装就行

> 💡 这一步可能要花上 1-2 小时，取决于你的网速，可以先去做点别的事。

---

## 第二步：安装 GitHub Desktop（用来"下载"代码，不用打命令行）

代码托管在 GitHub 这个网站上，"下载代码"这个动作专业说法叫 **clone**（克隆）或者 **pull**（拉取）。正常这一步需要在「终端」里敲命令，但如果你完全不想碰命令行，可以用一个有图形界面的工具：

1. 打开浏览器，访问 <https://desktop.github.com>
2. 下载并安装 **GitHub Desktop**（Mac 版）
3. 打开它，如果有 GitHub 账号就登录；没有的话点「创建账号」，免费注册一个就行（这一步只是为了后面能顺利下载代码，不注册也能下载公开项目，但注册一下体验更顺畅）

---

## 第三步：把项目"下载"到你的电脑上

如果你已经收到了一份项目的压缩包（`.zip` 文件）：

1. 双击这个 zip 文件，Mac 会自动解压出一个文件夹（一般叫 `echo`）
2. 把这个文件夹放到一个你好找的位置，比如「文稿」或者「桌面」

如果你是要从 GitHub 网站上的仓库地址下载：

1. 在 GitHub Desktop 里，点左上角 **File → Clone Repository**
2. 粘贴项目的仓库地址（形如 `https://github.com/xxx/echo`），或者直接搜索
3. 选一个你电脑上的保存位置，点 **Clone**，等它下载完

> 以后如果项目有更新，想拿到最新代码，就是点 GitHub Desktop 里的 **Fetch origin / Pull origin** 按钮——这就是大家说的"pull 一下"，意思是"把远端仓库里比我这边新的内容拉下来"。

---

## 第四步：打开项目

1. 找到你解压/下载出来的那个 `echo` 文件夹
2. 打开这个文件夹，能看到一个图标像蓝色图纸的文件，名字是 **`echo.xcodeproj`**
3. 双击它，Xcode 会自动打开，并开始在后台"读取"这个项目（左侧会出现一堆文件列表，这是正常的）

第一次打开可能要等个十几秒到一分钟，Xcode 在后台做一些准备工作，不用管它，等就行。

---

## 第五步：让 Xcode 认识"你"（登录 Apple ID）

这一步是为了让 Xcode 知道用谁的身份来"签名"这个 App（苹果要求所有能运行在设备/模拟器上的 App 都要有一个身份签名，哪怕只是自己测试用）。

1. 在 Xcode 菜单栏，点击左上角 **Xcode → Settings...**（老版本 Xcode 里叫 **Preferences...**）
2. 切换到 **Accounts** 标签页
3. 点左下角的 **+** 号，选 **Apple ID**，登录你平时用的 Apple 账号（免费的，不需要付费开发者账号也能在自己电脑上测试）
4. 登录完成后关掉这个设置窗口

---

## 第六步：把项目"认领"成你自己的

因为这个项目原来是别人开发的，签名信息（谁的身份、App 的唯一编号）不是你的，需要改成你自己的，不然运行会报错。这个项目把这部分设置放进了一个单独的小文件里，你只需要复制一份、填两行字就行，不用在 Xcode 里翻来翻去点选项。

### 6-1. 先搞清楚自己的 "Team ID" 是什么

Team ID 是一串 10 位的字母数字组合，代表"这是你的 Apple 开发者身份"。找它的方法：

1. 在 Xcode 里，菜单栏点 **File → New → Project...**
2. 随便选一个模板（比如第一个 App），一路点 **Next**，项目名字随便起，保存位置随便选（这个项目只是临时用来"看一眼"，等下会删掉）
3. 新项目打开后，点左侧文件列表最顶上的项目图标，再点中间面板上的 **Signing & Capabilities** 标签
4. 找到 **Team** 这一栏，点开下拉菜单，选择你刚才登录的 Apple 账号
5. 选中后，这一栏会显示成类似 **"你的名字 (A1B2C3D4E5)"** 的样子，**括号里那 10 位字符就是你的 Team ID**，把它记下来（截图或者抄在备忘录里都行）
6. 记下来之后，这个临时项目就没用了，可以直接删掉（在 Finder 里找到它拖进废纸篓即可），**不会影响到 Echo 项目**

### 6-2. 复制配置文件

1. 在 Finder 里打开 Echo 项目文件夹，找到里面的 **`Config`** 文件夹
2. 里面有个文件叫 **`Local.xcconfig.example`**，选中它，按 `Cmd + C` 复制，再按 `Cmd + V` 粘贴，Finder 会生成一份叫 "Local.xcconfig example 2" 之类名字的副本
3. 把这份副本重命名为 **`Local.xcconfig`**（去掉 `.example`，保留最前面的 `Local` 和最后面的 `.xcconfig`，中间不要有多余的字）

### 6-3. 编辑配置文件

1. 右键点击刚才重命名好的 `Local.xcconfig`，选择 **打开方式 → 文本编辑（TextEdit）**（或者其他任何纯文本编辑器）
2. 找到这一行：`DEVELOPMENT_TEAM =`，在等号后面粘贴上你刚才记下的 Team ID，比如改成：`DEVELOPMENT_TEAM = A1B2C3D4E5`
3. 找到这一行：`PRODUCT_BUNDLE_IDENTIFIER = rkny6.echo`，把 `rkny6` 换成任何你自己起的、独一无二的名字，比如改成：`PRODUCT_BUNDLE_IDENTIFIER = zhangsan.echo`
4. 保存文件（`Cmd + S`），关闭

> 💡 **Team** 就是"这是谁的身份"，**Bundle Identifier** 就是"这个 App 在全世界的唯一编号"，两个人不能用同一个编号，所以必须改成你自己的。这两项现在都存在 `Local.xcconfig` 这个文件里，它不会被提交回项目仓库，你怎么改都不会影响到原作者或者其他 clone 这个项目的人。

> ⚠️ 如果你已经打开了 Echo 项目、并且不小心在 Echo 项目的 **Signing & Capabilities** 面板里点过 Team 或改过 Bundle Identifier，建议关掉 Xcode，把 `Local.xcconfig` 按上面的步骤重新配置好之后再打开，避免两边设置打架。

---

## 第七步：运行起来！

1. 在 Xcode 顶部，找到一个类似"设备选择器"的下拉菜单（一般写着 `iPhone 16` 之类的名字），点开选一个你喜欢的 iPhone 型号——这是"模拟器"，也就是在电脑上模拟出一台虚拟 iPhone，不需要真的有一台 iPhone
2. 点击左上角那个 **▶️（播放/运行）按钮**
3. 第一次运行会比较慢（可能几分钟），Xcode 要把代码"翻译"成 App（这个过程叫**编译 / build**），下方状态栏会显示进度
4. 编译完成后，会自动弹出一个"模拟器"窗口，长得就像一台 iPhone，然后 App 会自动打开——如果看到界面了，恭喜你，跑起来了 🎉

如果中途弹出红色错误提示，先看下面的「常见问题」。

---

## 第八步（可选）：装到你真正的 iPhone 上

如果模拟器已经满足你，可以跳过这步。想装到真机上：

1. 用数据线把你的 iPhone 连接到 Mac
2. 手机上如果弹出"是否信任这台电脑"，点信任
3. 在 Xcode 顶部的设备选择器里，这次选你的真机名字（而不是模拟器）
4. 点 ▶️ 运行
5. 第一次会提示需要在手机上「信任开发者证书」：打开 iPhone 的 **设置 → 通用 → VPN 与设备管理**，找到你的 Apple ID，点"信任"
6. 再回 Xcode 点一次 ▶️ 就能装上了

> ⚠️ 免费 Apple ID 装到真机上的 App，**7 天后会自动过期打不开**，到时候重新连电脑用 Xcode 点一次 ▶️ 重新安装就行。如果不想每周都重装一次，需要花 688 元/年加入苹果开发者计划（Apple Developer Program），这个不是必须的，纯粹是免费方案的一个小限制。

---

## 第九步：想让它真的"聊天"，需要配置一个 AI 服务

App 打开之后是空壳子，要让它能跟你对话，需要告诉它去问哪个"AI大脑"。这一步在 App 里操作，不用碰 Xcode 了：

1. 打开 App，进入 **设置 → API 配置**
2. 需要填三样东西：
   - **Base URL**：AI 服务商给你的一个网址，相当于"去哪个地址问问题"
   - **Model**：选用哪个"AI模型"，相当于"问哪个大脑"，不同模型效果和价格不一样
   - **API Key**：一长串字符，相当于你专属的"通行证"，证明是你在使用这个服务，别人拿不到

这三样东西需要你自己去某个 AI 服务商（比如 OpenAI，或者国内的一些兼容服务）的网站上注册账号后获取，不同服务商注册流程不一样，这里不展开。拿到后填进去、保存，就可以正常聊天了。

> 如果不知道从哪弄这三样东西，可以在项目的 Issues 里搜一下有没有人问过同样的问题，或者直接提一个新 Issue 问。

---

## 常见问题

**Q: 打开项目或点运行时提示找不到 "Local.xcconfig"？**
说明第六步还没做完，回去检查 `Config` 文件夹里是不是已经有一份改名叫 `Local.xcconfig`（而不是 `Local.xcconfig.example`）的文件。

**Q: 点运行后一直转圈/卡住不动？**
第一次编译确实会比较久（几分钟属于正常），可以看下方状态栏的进度条有没有在动。如果卡了 10 分钟以上完全没反应，尝试点菜单栏 **Product → Clean Build Folder**，然后再运行一次。

**Q: 提示 "Signing for 'echo' requires a development team"？**
说明 `Config/Local.xcconfig` 里的 `DEVELOPMENT_TEAM` 还是空的，回到第六步把你的 Team ID 填进去。

**Q: 提示 Bundle Identifier 冲突/重复？**
打开 `Config/Local.xcconfig`，把 `PRODUCT_BUNDLE_IDENTIFIER` 改得更独特一点，比如加上今天的日期或者一串随机数字。

**Q: 打开项目后左侧文件列表是红色的？**
说明有文件缺失或者路径不对，一般是解压/下载不完整导致的，可以尝试重新下载一遍完整的项目文件夹。

**Q: 我该去哪里问其他问题？**
去项目的 GitHub 页面点 **Issues**，看看有没有人问过类似问题；没有的话可以照着 [Bug 反馈模板](.github/ISSUE_TEMPLATE/bug_report.md) 的格式提一个新的，尽量把报错的完整文字或截图贴上去，方便别人帮你看。
