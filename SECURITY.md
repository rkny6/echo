# 安全政策（Security Policy）

## 支持的版本

目前项目处于早期开发阶段（未发布正式版本），安全工作集中在 `main` 分支：

| 版本 | 支持状态 |
|------|----------|
| `main`（开发版） | ✅ 积极维护 |

## 报告安全漏洞

我们非常重视安全问题。**请勿在公开的 Issues 中报告安全漏洞**。

### 报告方式

- 私信联系项目维护者 GitHub 账号 [rkny6](https://github.com/rkny6)
- 或发送邮件至维护者邮箱 [rokunoy@outlook.com](mailto:rokunoy@outlook.com)

### 报告内容请包含

- 漏洞类型（如：信息泄露、注入、越权访问等）
- 受影响文件或模块
- 复现步骤
- 潜在影响
- （可选）修复建议

### 处理流程

1. 收到报告后 48 小时内确认收悉。
2. 评估影响范围与严重程度。
3. 制定修复方案并合入 `main` 分支。
4. 修复发布后在 CHANGELOG 中公开致谢（如报告者同意）。

---

## 数据与隐私安全说明

Echo 处理健康数据（HealthKit）等敏感信息。请所有贡献者注意：

- **API Key 安全**：API 密钥（LLM、天气、图像识别等）必须通过 Keychain 或用户配置存储，**严禁硬编码或提交到版本库**。
- **本地数据**：用户聊天记录、健康数据、个人资料仅存储于本地 SwiftData，不应上传到任何未声明的服务器。
- **日志脱敏**：新增日志时，避免输出完整 API Key、Token、用户原始健康数据。
- **MCP 安全**：接入远程 MCP 服务器时，注意鉴权与会话安全，避免将用户敏感上下文发送给不可信服务器。

## 依赖安全

- 本项目（目前）无第三方 Swift Package 依赖，使用系统框架（SwiftUI / SwiftData / HealthKit）。
- 若未来引入依赖，请确保来源可信并跟踪其安全公告。