# 隐私政策（Privacy Policy）

最后更新：2026-08

## 概述

echo 是一个开源的 iOS 虚拟陪伴应用，**没有自己的后端服务器**。你安装并运行这个 App 时，数据的默认去向是**你自己设备上的本地存储**；只有在你主动配置了某项第三方服务后，相应的数据才会发送给你自己选择的服务商。本文档说明 echo 会处理哪些数据、存放在哪里、什么情况下会离开你的设备，以及你可以如何控制这些行为。

如果你是从别人分发的构建产物（而非自己编译）安装的 echo，或者你 fork 本项目后自己修改、分发给其他人使用，实际的隐私表现取决于该构建/该分发者的具体实现，本政策仅描述本仓库源码的默认行为，见文末「关于 Fork 与二次分发」。

> 本文档只覆盖数据处理相关的内容。项目的免责条款（早期项目风险、非专业医疗/心理建议、第三方服务费用、未成年人使用等）请见 [README 的「⚠️ 免责声明」](README.md#️-免责声明请务必阅读)。

## 我们收集的数据类型

以下数据默认**只存储在你的设备本地**（SwiftData），不会同步到任何开发者运营的服务器 —— 因为本项目根本没有这样的服务器：

| 数据类型 | 用途 | 存储方式 |
|----------|------|----------|
| 聊天记录、对话摘要 | 维持对话上下文、生成长期记忆 | 本地 SwiftData |
| 用户画像 / 角色设定 | 个性化对话与主动关怀 | 本地 SwiftData |
| 日记内容 | 每日对话回顾 | 本地 SwiftData |
| 健康数据（步数、睡眠、心率变异性、生理周期等） | 通过 HealthKit 读取，用于健康事件检测与主动关怀 | 仅在内存/本地处理，见下文 HealthKit 专项说明 |
| 地理位置 | 天气感知、部分主动关怀场景 | 仅在需要时读取，用于生成天气请求 |
| 图片消息 | 聊天中的图片输入 | 本地存储；仅在你开启云端图像识别时才会上传 |
| API Key / Token | 访问你自己配置的第三方服务 | 存储在系统 **Keychain**，不写入源码或明文配置文件 |

echo 本身**不包含任何分析、埋点或广告 SDK**，不会为了统计使用行为而收集数据。

## HealthKit 专项说明

- echo 仅在你明确授权后才能读取对应的健康数据类型（步数、睡眠分析、心率变异性、生理周期记录等），权限粒度由 iOS 系统的健康权限弹窗控制，你可以逐项授权或拒绝。
- 读取到的健康数据仅用于本机的事件检测（例如"今天步数明显偏少"）与生成关怀文案，不会被用于广告定向，也不会被出售或转让给第三方。
- 如果生成关怀文案时使用了云端 LLM（见下文），健康事件的**摘要性描述**（而非原始健康数据）可能作为对话上下文的一部分发送给你配置的 LLM 服务商——是否发生取决于 `PromptBuilder` 在当轮对话中是否引用了健康上下文，你可以随时在系统设置中关闭对应的 HealthKit 权限来杜绝这一路径。

## 哪些情况下数据会离开你的设备

echo 默认不联网发送任何个人数据。只有当你在「设置」中主动配置了以下任一项，对应类型的数据才会发送给**你自己选择/申请的第三方服务商**（而不是发给项目开发者）：

| 你配置的功能 | 会发送什么 | 发送给谁 |
|--------------|-----------|----------|
| 云端 LLM（设置 → API 配置） | 对话历史、记忆摘要、日常上下文等文本 | 你填写的 Base URL 对应的服务商（OpenAI / Azure / 自建服务等） |
| 云端图像识别（Agnes） | 你发送的图片 | Agnes 图像识别服务 + ImgBB（图片中转） |
| 天气感知 | 粗粒度地理位置坐标 | 彩云天气（如果配置了 `CaiyunWeatherToken`）或 [Open-Meteo](https://open-meteo.com/)（默认 fallback，无需密钥） |
| 远程 MCP 工具 | 视具体工具而定，可能包含对话上下文中与工具调用相关的字段 | 你在「设置 → 高级选项」中填写的 MCP 服务器地址 |

这些第三方服务各自有自己的隐私政策，echo 无法替它们做出承诺，请在配置前自行查阅对应服务商的条款。

## 你的控制权

- 以上所有第三方功能均为**可选**，未配置时功能会自动降级（例如天气会走免密钥的 Open-Meteo）或直接跳过（例如图像识别退回本地 Vision）。
- 你可以随时在系统设置中撤销 HealthKit、定位、相册等权限，echo 会相应地失去对应的感知能力，但不影响核心聊天、记忆、日记功能。

### 本地数据的保留与丢失

echo 当前没有 iCloud 或自有服务器同步，聊天记录、记忆、日记、个人资料和头像等数据主要保存在 App 的本地沙盒中。数据是否保留取决于安装方式：

- **直接从 Xcode 点击 Run 安装或重新安装**：只要 Bundle Identifier 没有改变，通常属于覆盖更新，会保留原有 App 沙盒数据，包括聊天记录。
- **从 App Store 或其他渠道更新 App**：正常更新会保留 App 数据；但如果构建使用了不同的 Bundle Identifier，就会被系统视为另一个 App。
- **在手机上删除 App**：会删除本地 SwiftData 数据库和其他 App 沙盒文件。重新安装后无法从 echo 的服务器恢复，因为 echo 没有保存远端副本。
- **iOS 的“卸载 App”（Offload App）**：通常会移除 App 本体但保留用户数据；重新安装同一 App 后系统通常会恢复这些数据。为避免误操作，重要数据仍建议备份。
- **修改 Bundle Identifier、抹掉设备内容、手动删除 App 数据，或数据库迁移失败触发重置**：都可能导致原有数据不可用或丢失。

因此，开发调试时可以直接用 Xcode 覆盖安装，但不要先删除手机上的 App，也不要随意修改 Bundle Identifier。项目处于早期迭代阶段，请自行做好重要数据备份。

## 未成年人

echo 不专门面向儿童设计，也没有年龄校验机制。如果监护人允许未成年人使用，请自行评估 HealthKit、聊天记录等数据的处理是否符合当地对未成年人数据保护的要求。

## 关于 Fork 与二次分发

echo 是按 [MIT License](LICENSE) 开源的项目，任何人都可以 fork、修改并自行分发。**本政策只描述本仓库当前源码的默认行为**：

- 如果你 fork 本项目并加入了自己的后端、埋点、云同步等功能，你需要自行撰写并维护适用于你那个版本的隐私政策，并对你的用户负责。
- 如果你计划将 echo（或基于它的衍生版本）上架 App Store，苹果会要求你自己撰写与实际数据处理行为一致的隐私政策与"隐私养分标签"（Privacy Nutrition Label），本文档可以作为起点，但请根据你的实际改动核实每一项。

## 政策变更

本政策可能随功能迭代更新，重大变更会体现在 [CHANGELOG.md](CHANGELOG.md) 中。

## 联系方式

对本政策有疑问，或希望报告与隐私相关的问题，请通过 [rokunoy@outlook.com](mailto:rokunoy@outlook.com) 联系项目维护者；如果是安全漏洞，请参见 [SECURITY.md](SECURITY.md) 中的报告方式，不要在公开 Issue 中披露。
